<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-3">
        <div class="w-full flex item justify-end">
            <a class="bg-red-500 p-1 rounded-md" href="/logout">logout</a>
        </div>
        <h3 class="text 3xl "><span class="font-light">
                <h2>Hello,</h2>
            </span>
            <%= user.name %>
        </h3>
        <h3 class="m-2 mb-3">Create a new post</h3>
        <form method="post" action="/post">
            <textarea placeholder="whats in your mind..."
                class="w-1/3 m-2 p-3 bg-transparent border-2 border-zinc-8000 block" name="content"></textarea>
            <input class="bg-blue-600 p-1 m-2 rounded-md" type="submit" value="create new post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your posts.</h3>

            <div class="postcontainer   mt-5 mb-2">
                <% user.posts.reverse().forEach(function(post){%>
                    <div class="post w-1/3 p-4 mb-3 border-2 border-zinc-700 bg-zinc-800 ">
                        <h3 class="text-blue-600">@<%=user.username%></h4>
                            <p><%=post.content%></p>
                            <small class="mt-2 "><%=post.likes.length %></small>
                            <div class="btn flex mt-2 gap-4">
                                <a class="text-blue-400" href="/like/<%=post._id %>">
                                     <%= post.likes.indexOf(user._id)==-1 ? "like" : "unlike" %> </a>
                                <a class="text-zinc-400" href="/edit/<%= post._id%>">edit</a>

                            </div>

                    </div>

                    <% })%>


            </div>
        </div>
    </div>
</body>

</html>